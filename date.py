import random

def find_place (list_places):
    index = randint(1, len(list_places))
    return (list_places[index])


list_date = ['ШреддерПицца', 'ХанБуз', 'АртПаб', 'Гуси', 'Камин', 'RedRabbit', 'Shurubor']
